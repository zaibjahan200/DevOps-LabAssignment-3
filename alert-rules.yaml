groups:
  - name: apache-alerts
    rules:
      - alert: HighTraffic
        expr: rate(apache_accesses_total[1m]) > 10
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High Apache traffic detected"